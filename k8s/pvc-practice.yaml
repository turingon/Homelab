apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-practice
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  storageClassName: sc-practice
