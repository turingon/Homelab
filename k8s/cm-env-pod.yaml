apiVersion: v1
kind: Pod
metadata:
  name: dapi-test-pod
spec:
  containers:
    - name: test-container
      image: registry.k8s.io/busybox:1.27.2
      envFrom:
      - configMapRef:
          name: test-cm
      command: ["/bin/sh", "-c", "env" ]
  restartPolicy: Never

